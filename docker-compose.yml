services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - SESSION_SECRET=${SESSION_SECRET}
      - PORT=3001
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data:
